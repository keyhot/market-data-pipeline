"""Phrase-bank commentary — STUB (Task 2 scaffold). Tasks 4/5 replace this with
deterministic per-personality, per-event, per-tier phrase banks with
anti-repetition. Stays quiet by default (the ambient register)."""


def lines_for_tick(state, dir_state, now, config):
    return []
